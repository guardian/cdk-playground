stacks:
  - deploy
regions:
  - eu-west-1
deployments:
  cloudformation:
    type: cloud-formation
    app: cdk-playground
    parameters:
      # inntentionally breaking to test https://github.com/guardian/riff-raff/pull/668
      # templatePath: CdkPlayground.template.json
      amiParameter: AMICdkplayground
      amiEncrypted: true
      amiTags:
        Recipe: arm64-bionic-java11-deploy-infrastructure
        AmigoStage: PROD
        BuiltBy: amigo
  cdk-playground:
    type: autoscaling
    parameters:
      bucket: deploy-tools-dist
    dependencies:
      - cloudformation
